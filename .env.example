# Example environment configuration for astro_utils
# Copy this file to .env and customize the paths for your system

# Base directory for all astronomy data (required)
# All other paths will be derived from this if not explicitly set
ASTRO_DATA_DIR=/path/to/your/data

# R21 data download URL (optional, will prompt if not set)
R21_URL=https://cral-perso.univ-lyon1.fr/labo/perso/johan.richard/MUSE_data_release/

# Optional: Override specific directories
# Uncomment and customize these if you want to use non-standard locations

# R21 spectra directory
# R21_SPECTRA_DIR=/custom/path/to/muse_catalogs/spectra

# R21 catalog directory
# R21_CATALOG_DIR=/custom/path/to/muse_catalogs/catalogs

# Source spectra directory
# SOURCE_SPECTRA_DIR=/custom/path/to/source_spectra
